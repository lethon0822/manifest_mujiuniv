apiVersion: v1
kind: Service
metadata:
  name: mujiuniv-user
  namespace: app
  labels:
    app: mujiuniv-user
spec:
  selector:
    app: mujiuniv-user
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP